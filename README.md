# NFL-php
The home page displays all the NFL teams in the dataase with a link to their website.

A user can create a new NFL team to add to the data base.

A user can read all the entries in the database.

A user can update a NFL teams information in the database.

A user can delete a NFL team out of the database.
